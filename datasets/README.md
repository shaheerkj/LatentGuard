# datasets/

Source datasets for autoencoder training and end-to-end testing.

| Dataset | Use | License |
| --- | --- | --- |
| CSIC 2010 HTTP | Primary autoencoder training (benign split) + attack replay | Open |
| HTTP Params Dataset | Secondary attack corpus (XSS / SQLi / cmdi) | Open |
| DVWA captured traffic | App-specific benign baseline | Self-generated |

Raw datasets are gitignored; only the download/split scripts are tracked.
